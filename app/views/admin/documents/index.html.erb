<p class="bordered"><%= link_to 'Upload File', new_admin_document_path %></p>

<table>
  <tr>
    <th>Filename</th>
    <th>Size</th>
    <th>Content Type</th>
    <th></th>
  </tr>
  <% for document in @documents %>
    <tr class="<%= cycle("", "alternate") %>">
      <td><%= link_to "#{document.paperclip_file_name}", document.paperclip.url %></td>
      <td><%= document.paperclip_file_size %></td>
      <td><%= document.paperclip_content_type %></td>
      <td><%= link_to 'Delete', admin_document_path(document), :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
