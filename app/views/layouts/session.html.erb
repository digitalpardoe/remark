<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
  <title><%= name %></title>
  <%= less_link_tag 'session/screen' %>
  <%= javascript_include_tag	:defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<% flash.each do |key, msg| %>
		<%= content_tag :div, content_tag(:p, msg), :id => key %>
	<% end %>
  
	<%= yield %>
</body>
</html>
