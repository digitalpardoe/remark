<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
  <title><%= (yield :title).empty? ? name : (yield :title) %></title>
  <%= stylesheet_link_tag "http://fonts.googleapis.com/css?family=PT+Sans:400,700,400italic|PT+Serif:700" %>
  <%= less_link_tag :all %>
  <%= javascript_include_tag :defaults %>
	<!--[if lt IE 9]>
		<script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
	<![endif]-->
  <%= csrf_meta_tag %>
	<%= auto_discovery_link_tag %>
</head>
<body>
	<header>
		<h1><%= link_to name, root_path %></h1>
		<p><%= subtitle %></p>
	</header>
	
	<nav>
		<%= navigation %>
	</nav>
	
	<div id="content">
  	<% flash.each do |key, msg| %>
			<%= content_tag :div, content_tag(:p, msg), :id => key %>
		<% end %>
  	
  	<%= yield %>
	</div>
	
	<footer>
	  <p>Powered By <a href="https://github.com/digitalpardoe/remark">Remark</a></p>
	</footer>

  <%= tracking %>
  
</body>
</html>
