{
  "image": "ruby:2.7.8",
  "forwardPorts": [3000],
  "postCreateCommand": "apt update && apt dist-upgrade -y && apt install -y nodejs && gem update --system 3.3.22 &&bundle config set --local without 'production' && bundle install"
}